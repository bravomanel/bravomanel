function sum (a, b) {
    return a + b;
}
function subtract (a, b) {
    return a - b;
}
function multiply (a, b) {
    return a * b;
}
function divide (a, b) {
    return a / b;
}
function operate(operator, a, b) {
    operator(a,b);
}

// let container = document.querySelector('.digitsContainer')
// for (let digit of board) {
//     let itsId = digit.id;
//     digit.style.gridArea = itsId;
// }

